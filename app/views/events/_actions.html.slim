= link_to 'Редактировать', edit_event_path(@event) if can? :update, @event
|  - 
= link_to 'Удалить', @event, :method => :delete, :confirm => 'Уверены..?' if can? :destroy, @event
|  - 
= link_to 'Назад', events_path